{
  "name": "raftly-bloated-api",
  "version": "1.0.0",
  "description": "Raftly Fleet Dispatch API — over-built, needs scope cutting",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --testEnvironment=node"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
